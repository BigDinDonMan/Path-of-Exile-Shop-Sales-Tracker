package poedatatracker.gui.display;

import javafx.scene.control.ListCell;
import poedatatracker.core.models.CurrencyExchange;

public class CurrencyExchangeListCell extends ListCell<CurrencyExchange> {



    @Override
    protected void updateItem(CurrencyExchange ex, boolean empty) {
        super.updateItem(ex, empty);
    }
}
